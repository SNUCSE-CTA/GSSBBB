# Graph Similarity Search

Graph similarity search using Binary Branch and Bound (BBB) with dynamic Hungarian and DFS for efficiency.

Build and run:
```
mkdir build
cd build
cmake ..
make
./GraphSimilaritySearch <query graph> <database> <tau>
```
- note: research log marker 94
